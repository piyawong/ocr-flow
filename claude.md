# Claude AI Assistant - กฎและแนวทางการทำงาน

> **สำหรับ:** Claude Code AI Assistant
> **อัปเดตล่าสุด:** 2025-12-14

---

## 📖 กฎบังคับก่อนเริ่มงาน (MANDATORY)

### ⚠️ กฎที่ 1: อ่าน STRUCTURE.md ก่อนทุกครั้ง

**ก่อนทำงานกับ project นี้ทุกครั้ง** ให้ทำตามขั้นตอนนี้:

1. ✅ **ตรวจสอบว่าเคยอ่าน STRUCTURE.md ใน session นี้หรือยัง**
   - ถ้า **ยังไม่เคยอ่าน** → อ่าน `STRUCTURE.md` ก่อนทำอะไร
   - ถ้า **อ่านแล้ว** → ดำเนินการต่อได้เลย

2. ✅ **เมื่อผู้ใช้ถามคำถามหรือให้ task ใหม่**
   - ถ้าคำถามเกี่ยวกับโครงสร้าง/logic ของ project → อ้างอิง STRUCTURE.md
   - ถ้าคำถามเกี่ยวกับ auto label PDF / pattern matching → อ้างอิง `auto-label.md`

### ⚠️ กฎที่ 2: อัปเดต STRUCTURE.md เมื่อมีการเปลี่ยนแปลง

**เมื่อทำการแก้ไข/เพิ่มเติม code ที่ส่งผลกระทบต่อ:**
- ✅ โครงสร้างโฟลเดอร์/ไฟล์
- ✅ Backend modules/services/controllers
- ✅ Frontend pages/components
- ✅ Database schema
- ✅ Infrastructure (Docker, environment variables)
- ✅ Logic หลักหรือ data flow
- ✅ Templates configuration (Database)

**ต้องอัปเดต STRUCTURE.md ทันที** โดย:
1. แก้ไขส่วนที่เกี่ยวข้องใน STRUCTURE.md
2. อัปเดตวันที่ "อัปเดตล่าสุด" ที่ด้านบนของเอกสาร
3. ถ้า auto label logic เปลี่ยนแปลง → อัปเดต `auto-label.md` ด้วย (ถ้าจำเป็น)

---

## 📋 แนวทางการทำงาน

### 1. เมื่อได้รับ Task ใหม่

```
ขั้นตอนการทำงาน:
1. อ่าน STRUCTURE.md (ถ้ายังไม่เคยอ่านใน session นี้)
2. ทำความเข้าใจ task ที่ได้รับ
3. ค้นหาข้อมูลที่เกี่ยวข้องใน:
   - STRUCTURE.md (โครงสร้าง, modules, logic)
   - auto-label.md (auto label PDF / pattern matching logic)
   - template-learning-task.md (template optimization จาก manual labels)
4. วางแผนการทำงาน (ใช้ TodoWrite ถ้าจำเป็น)
5. ดำเนินการตาม task
6. อัปเดต STRUCTURE.md (ถ้ามีการเปลี่ยนแปลง)
7. ทดสอบและรายงานผล
```

### 2. เมื่อทำงานกับ Backend (NestJS)

- **อ่าน:** [สถาปัตยกรรม Backend](./STRUCTURE.md#สถาปัตยกรรม-backend)
- **Module ที่มี:**
  - `files/` - จัดการไฟล์ (upload + grouping)
  - `labeled-files/` - จัดการไฟล์ที่ label แล้ว
  - `templates/` - จัดการ templates (Database-based)
  - `task-runner/` - รัน background tasks
  - `label-runner/` - รัน label process
  - `parse-runner/` - รัน parse data process
  - `minio/` - MinIO integration
- **ถ้าเพิ่ม module/service ใหม่:** อัปเดต STRUCTURE.md ทันที

### 3. เมื่อทำงานกับ Frontend (Next.js)

- **อ่าน:** [สถาปัตยกรรม Frontend](./STRUCTURE.md#สถาปัตยกรรม-frontend)
- **Stages:**
  - `01-raw/` - อัพโหลดไฟล์
  - `02-group/` - จัดกลุ่มไฟล์
  - `03-pdf-label/` - label PDF
  - `04-extract/` - extract ข้อมูล
  - `05-review/` - review
  - `06-upload/` - upload final
- **ถ้าเพิ่ม page/component ใหม่:** อัปเดต STRUCTURE.md ทันที

### 4. เมื่อทำงานกับ Database

- **อ่าน:** [Database Schema](./STRUCTURE.md#database-schema)
- **Tables:**
  - `files` - ไฟล์ทั้งหมด (upload + grouping metadata)
  - `groups` - metadata ของ groups
  - `labeled_files` - ไฟล์ที่ label แล้ว
  - `templates` - template configurations
- **ถ้าเปลี่ยน schema:** อัปเดต STRUCTURE.md ทันที

### 5. เมื่อทำงานกับ Auto Label PDF / Pattern Matching

- **อ่าน:** `auto-label.md` (อ่านทั้งหมด)
- **Templates:** เก็บใน Database (table `templates`) - จัดการผ่าน `/templates` page
- **สิ่งสำคัญ:**
  - **Exact Match Only** (normalized text comparison - lowercase + trim + collapse whitespace)
  - AND logic ภายใน variant (ต้องเจอทุกคำ)
  - OR logic ระหว่าง variants (เจอ variant ใดก็ได้)
  - Negative patterns (ป้องกัน false match)
  - Single page vs Multi-page detection
  - Sequential processing (currentTemplate tracking)
- **ถ้าแก้ไข logic:** อัปเดตทั้ง STRUCTURE.md และ auto-label.md

### 6. เมื่อต้องการ Template Optimization (เรียนรู้จาก Manual Labels)

- **อ่าน:** `template-learning-task.md` (อ่านทั้งหมด)
- **Use Case:** User manual label แล้วต้องการให้ระบบเรียนรู้และปรับปรุง templates
- **Workflow:**
  1. User manual label หน้าที่ auto-label ไม่ติด
  2. Claude อ่าน manual labels จาก API
  3. Claude วิเคราะห์ ลักษณะ pattern เอกสาร จาก OCR text และบางที ดูจากรูปภาพ ดูลักษณะเอกสาร และหา patterns
  4. Claude คิดวิเคราะ แนะนำ patterns ให้ user review หรือ แนะนำ logic เพิ่มเติม เช่น เพิ่ม algorithm นี้ หรือเพิ่ม step นี้ lib นี้ 
  5. Claude สร้าง/อัปเดต template
  6. Claude ทดสอบด้วย relabel และรายงานผล
- **สิ่งสำคัญ:**
  - **ต้องแสดง recommended patterns ให้ user review ก่อน**
  - เลือก patterns ที่เฉพาะเจาะจง (ไม่กว้างเกินไป)
  - เลือก patterns ที่คงที่ (ไม่ใช่ข้อมูลผันแปร)
  - ตรวจสอบ negative patterns เพื่อป้องกัน false match
  - ทดสอบด้วย relabel หลังอัปเดต template

### 7. เมื่อทำงานกับ Infrastructure

- **อ่าน:** [Infrastructure](./STRUCTURE.md#infrastructure)
- **Services:**
  - PostgreSQL (port 5434)
  - MinIO (port 9004, 9005)
  - Backend (port 4004)
  - Frontend (port 3004)
- **ถ้าเปลี่ยน Docker/environment:** อัปเดต STRUCTURE.md ทันที

---

## 🔍 คำถามที่พบบ่อย (FAQ)

### Q1: โครงสร้างโฟลเดอร์ของ project เป็นอย่างไร?
**A:** อ่าน [โครงสร้างโฟลเดอร์](./STRUCTURE.md#โครงสร้างโฟลเดอร์)

### Q2: Backend มี module อะไรบ้าง?
**A:** อ่าน [สถาปัตยกรรม Backend](./STRUCTURE.md#สถาปัตยกรรม-backend)

### Q3: Frontend มีหน้าอะไรบ้าง?
**A:** อ่าน [สถาปัตยกรรม Frontend](./STRUCTURE.md#สถาปัตยกรรม-frontend)

### Q4: Database มี table อะไรบ้าง?
**A:** อ่าน [Database Schema](./STRUCTURE.md#database-schema)

### Q5: Logic การ auto label PDF ทำงานอย่างไร?
**A:** อ่าน `auto-label.md` (ทั้งหมด)

### Q6: Templates จัดการอย่างไร?
**A:** Templates เก็บใน Database (table `templates`) - ดู [Template Structure](./STRUCTURE.md#template-structure-database) และจัดการผ่าน `/templates` page

### Q7: Pattern matching ทำงานอย่างไร?
**A:** อ่าน `auto-label.md` (ส่วน "Pattern Matching Strategy")

### Q8: User manual label แล้ว อยากให้ระบบเรียนรู้และปรับปรุง templates ทำอย่างไร?
**A:** อ่าน `template-learning-task.md` (ทั้งหมด) - อธิบาย workflow การวิเคราะห์ manual labels และสร้าง/อัปเดต templates อัตโนมัติ

### Q9: เป้าหมายของ project นี้คืออะไร?
**A:** อ่าน [เป้าหมายและวัตถุประสงค์](./STRUCTURE.md#เป้าหมายและวัตถุประสงค์)

---

## ✅ Checklist สำหรับ Claude

### ก่อนเริ่มงาน
- [ ] อ่าน STRUCTURE.md แล้วหรือยัง? (ถ้ายัง → อ่านก่อน)
- [ ] เข้าใจ task ที่ได้รับแล้วหรือยัง?
- [ ] ตรวจสอบเอกสารที่เกี่ยวข้องแล้วหรือยัง? (STRUCTURE.md, auto-label.md, template-learning-task.md)

### หลังทำงานเสร็จ
- [ ] ทดสอบการทำงานแล้วหรือยัง?
- [ ] มีการเปลี่ยนแปลงที่ต้องอัปเดต STRUCTURE.md หรือไม่?
  - [ ] โครงสร้างโฟลเดอร์/ไฟล์
  - [ ] Backend modules/services
  - [ ] Frontend pages/components
  - [ ] Database schema
  - [ ] Infrastructure
  - [ ] Logic หลัก
- [ ] ถ้ามี → อัปเดต STRUCTURE.md แล้วหรือยัง?
- [ ] อัปเดตวันที่ "อัปเดตล่าสุด" แล้วหรือยัง?

---

## 📚 ไฟล์เอกสารสำคัญ

1. **STRUCTURE.md** ⭐ (อ่านก่อนทุกครั้ง)
   - โครงสร้างโค้ด
   - Backend/Frontend architecture
   - Database schema
   - Infrastructure
   - Logic และ data flow

2. **auto-label.md** ⭐
   - Logic การ auto label PDF (Stage 2)
   - Pattern matching algorithm (Exact Match)
   - Template structure และการทำงาน
   - AND/OR logic, Negative patterns
   - ตัวอย่าง flow และ troubleshooting

3. **template-learning-task.md** ⭐ (สำหรับ Template Optimization)
   - วิเคราะห์ manual labels และสร้าง/อัปเดต templates
   - Pattern selection guidelines
   - ตัวอย่างการวิเคราะห์ OCR text แบบละเอียด
   - Safety checks และ testing workflow
   - ใช้เมื่อ: user manual label แล้วต้องการให้ระบบเรียนรู้

4. **parse-data.md**
   - Logic การ parse data จาก OCR (Stage 3)
   - ตราสาร (foundation instrument)
   - บัญชีรายชื่อกรรมการ (committee members)

5. **task-runner.md**
   - Pattern สำหรับ Infinite Worker Loop
   - SSE Logging

6. **CLAUDE.md** (ไฟล์นี้)
   - กฎและแนวทางการทำงานสำหรับ Claude

---

## 🚫 สิ่งที่ห้ามทำ

1. ❌ **ห้าม** เริ่มทำงานโดยไม่อ่าน STRUCTURE.md
2. ❌ **ห้าม** แก้ไขโครงสร้างโดยไม่อัปเดต STRUCTURE.md
3. ❌ **ห้าม** สร้าง module/page ใหม่โดยไม่บันทึกใน STRUCTURE.md
4. ❌ **ห้าม** เปลี่ยน database schema โดยไม่อัปเดต STRUCTURE.md
5. ❌ **ห้าม** แก้ไข auto label / pattern matching logic โดยไม่อัปเดต auto-label.md

---

## 💡 Tips สำหรับ Claude

1. **อ่าน STRUCTURE.md เป็นประจำ** - เอกสารนี้คือแหล่งข้อมูลหลักของ project
2. **อ้างอิงเอกสาร** - เมื่อตอบคำถาม ให้อ้างอิง section ใน STRUCTURE.md หรือ auto-label.md
3. **อัปเดตเอกสารทันที** - อย่ารอให้โค้ดและเอกสารไม่ตรงกัน
4. **ใช้ TodoWrite** - ใช้ TodoWrite เพื่อวางแผนและติดตามงาน
5. **ทดสอบก่อนส่งมอบ** - ทดสอบการทำงานให้แน่ใจว่าถูกต้อง

---

**สร้างโดย:** OCR Flow Development Team
**อัปเดตล่าสุด:** 2025-12-14

---

## 🎯 สรุปกฎสำคัญ

```
1. อ่าน STRUCTURE.md ก่อนทุกครั้ง (ถ้ายังไม่เคยอ่านใน session นี้)
2. อัปเดต STRUCTURE.md ทุกครั้งที่มีการเปลี่ยนแปลง
3. อ้างอิงเอกสารเมื่อตอบคำถาม
4. ใช้ TodoWrite เพื่อจัดการงาน
5. ทดสอบก่อนส่งมอบ
```

**จำไว้:** STRUCTURE.md คือแหล่งข้อมูลหลักของ project นี้

** งานนี้ ถ้าต้องใช้ sub agent หรือใช้ sub agent ได้ ให้ใช้เลย **